# v1.5.0 实施清单审计

审计日期：2026-09-22。状态只基于仓库内可复核证据。

| 确认项 | 状态 | 证据 |
|---|---|---|
| 六个确定性 CLI/MCP 工具与 JSON Schema | 完成 | `schemas/tool-registry.json`、`scripts/rtk_tool.py`、`scripts/mcp_server.py` |
| 输入预检和不可绕过质量门 | 完成 | `services/quality.py`、失败用例测试 |
| 不可变快照与 SHA-256 证据链 | 完成 | RFC 8785 快照、输入/核数据/结果校验与篡改测试 |
| 区分计数不确定度与完整测量不确定度 | 完成 | 字段改名、分量清单、`combined_measurement_uncertainty_available=false` |
| 限制经验修正适用范围 | 完成 | 自定义源阻断、精确内置源哈希限制、结果中记录配置 |
| 负样本/缺参/干扰/盲测科学评测 | 部分完成 | 缺参、低计数、干扰与提示注入回归完成；独立盲样未提供 |
| 两个真实宿主 Skill 行为评测 | 待外部执行 | 已提供统一 MCP/CLI 和 `evaluation/cases.json`，但没有真实宿主轨迹 |
| `verify.py` 自动选择环境和依赖诊断 | 完成 | 普通 Python 可转入 `.runtime`；缺依赖时提示 `--install` |
| 无 Skill/说明型/完整工具型消融 | 待外部执行 | 已定义组别与指标；尚无真实模型运行记录 |
| 参赛指标表、工具轨迹与演示样例 | 部分完成 | 用例与指标已定义；外部盲样及宿主轨迹待采集 |

因此，本版本已经完成可移植确定性工具和科学约束层，但不能宣称已经完成独立计量学验证或跨宿主效果证明。后两项需要真实宿主版本、实验数据和原始运行轨迹。

## v1.5.1 复核增量（2026-09-23）

| 新评定意见 | 当前处理 | 证据与边界 |
|---|---|---|
| Web/桌面导出与 CLI/MCP 验证规则一致 | 已实现 | `services/report_validation.py` 为共同入口；无效/阻断快照拒绝正式导出，新增回归用例。哈希不构成数字签名。 |
| 条件性数值语义 | 已实现 | `estimated_activity_bq_kg` 保留过程估算；`reportable_activity_bq_kg` 仅在该核素就绪且检出时赋值。正式 PNG/PDF/Excel/模板只使用后者。 |
| 独立科学验证 | 评测入口已实现，实测未完成 | `evaluation/evaluate.py science` 需未调参盲样/标准物质及参考文件哈希；当前不提供虚构准确度。 |
| 两真实宿主与三组消融 | 轨迹清点入口已实现，实测未完成 | `evaluation/evaluate.py hosts` 保存提示、轨迹、输出 SHA-256；没有真实宿主运行记录，不得宣称效果。 |
| 每值核数据来源和用户声明粒度 | 已记录，权威逐值核对待完成 | 核数据表每值含来源核查指针与 `exact_value_verified=false`；证书和几何等为用户声明。旧数值未自动替换为新版评价。 |
| 刻度源缺采集时间 | 已降级 | 预检及计算质量门标为 `conditional_missing_standard_acquisition_time`；记录未应用衰变校正。 |

**仍需外部实验证据**：独立盲样/空白/低计数/干扰谱的参考结果、至少两个真实宿主的原始调用轨迹、经审核的逐值核数据。当前软件测试通过不等于这些工作已经完成。

## 2026-09-24 复核意见执行进度（未发布）

| 项目 | 当前状态 |
|---|---|
| 核数据逐值核查与参数敏感性 | ⁴⁰K 的半衰期、1460 keV 能量和发射概率对照 DDEP 2025；质量百分比换算单独重算，生产常数未替换。Ra/Th 子体线、分支和干扰峰仍未逐项核准，见 `NUCLEAR_DATA_AUDIT.md`。 |
| 独立谱科学验证 | 评测代码要求原始样品/刻度源谱与快照哈希一致，并记录外部独立刻度证据；公开候选筛选后仍无合格完整原始数据，真实指标待测。 |
| 双宿主三组消融 | 未取得第二真实宿主与完整原始轨迹，未运行；不能将 CLI/MCP 单元测试充作宿主效果。 |
| 固定参赛发布 | 有 Windows/CPython 3.12 精确依赖版本快照；由于独立评测与双宿主实测未完成，未创建“已验证”发布 commit，也未推送。 |
| 软件编译/回归 | `python scripts/verify.py` 编译通过，31 项测试通过；此结果只覆盖工程回归与合成数据。 |
